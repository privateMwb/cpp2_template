<!--
  Retargeting: replace every <angle-bracket> placeholder. Highlights,
  Performance, Release, and the Benchmarks table are project-specific
  — write them fresh per release, from real measured numbers and real
  shipped features, never invented. Testing and Continuous Integration
  already match this template's actual structure (test categories,
  build matrix) and only need updating if that structure changes.

  Filename must match the git tag exactly, e.g. tag "v1.0.0" ->
  ".github/releases/v1.0.0.md", since release.yml's body_path looks
  the file up by tag name.
-->

# <ProjectName> v1.0.0

The first stable release of **<ProjectName>**, <one-line description
of what it does and why>, for modern C++23.

## Highlights

<!-- The actual shipped features, in the order a user would care
     about them. Name specific decisions and APIs, not generic claims
     — the way "Configurable growth policy via GrowthNum/GrowthDen
     template parameters" says something the reader couldn't have
     guessed, but "fast and flexible" doesn't. -->

- **<Feature>** — <what it is and why it matters>.
- **<Feature>** — <what it is and why it matters>.

## Performance

<!-- The specific implementation decisions behind the Benchmarks
     numbers below — not a restatement of them. What was done to
     achieve that result, and why it works. -->

- <Design decision> — <what it avoids or achieves, and where>.
- <Design decision> — <what it avoids or achieves, and where>.

## Benchmarks

Measured against `<reference-implementation>` at 10K / 100K / 1M
iterations. Full results: `benchmarks/results/v1.0.0.md`.

| Operation | <ProjectName> (1M) | <reference-implementation> (1M) | Δ |
|---|---|---|---|
| `<operation>` | `<time>` | `<time>` | `<±N%>` |

<!-- 2-3 sentences: where the biggest wins are concentrated, and where
     the trade-offs are, in plain language. Real numbers pulled from
     the table above, not a generic "fast in most cases" summary. -->

## Testing

The project includes a comprehensive test suite — both the custom
framework and GoogleTest, covering the same categories — spanning:

- Unit tests
- Integration tests
- Lifecycle tests
- Regression tests
- Concurrency tests

<!-- Add project-specific coverage areas below the standard five
     above only if this release actually added something worth
     calling out (e.g. a new invariant, a new failure mode covered). -->

## Code Coverage

The current test suite achieves:

- **<N>% line coverage** (`<covered>` / `<total>` lines)
- **<N>% function coverage** (`<covered>` / `<total>` functions)

Coverage figures exclude test infrastructure, focusing on the
`<ProjectName>` library implementation.

## Continuous Integration

Automated builds and tests are configured for:

- GCC — Debug
- GCC — Release
- Clang — Debug
- Clang — Release
- MSVC — Debug
- MSVC — Release
- AppleClang — Debug
- AppleClang — Release

## Release

<!-- 2-3 sentences: what this release establishes or changes overall
     — the API surface, a design decision, or a milestone — not a
     restatement of the Highlights bullets. -->

This release represents <what this version establishes or changes>.

## Installation

**From source:**

```bash
git clone https://github.com/<owner>/<repo>.git
cd <repo>
cmake -B build \
  -DBUILD_TESTS=OFF \
  -DBUILD_BENCHMARKS=OFF \
  -DBUILD_REGRESSION=OFF \
  -DBUILD_EXAMPLES=OFF
cmake --install build
```

Then, in your own `CMakeLists.txt`:

```cmake
find_package(<ProjectName> CONFIG REQUIRED)
target_link_libraries(your_target PRIVATE <ProjectName>::<ProjectName>)
```

See the [README](https://github.com/<owner>/<repo>#readme) for the
full build matrix, vcpkg/Conan packaging status, and API reference.
