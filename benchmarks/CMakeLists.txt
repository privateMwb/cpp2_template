# ============================================================
# Build configuration for the benchmark suites.
# ============================================================

# Custom framework suite — see custom/README.md.
add_subdirectory(custom)

# Google Benchmark suite — see google_benchmark/README.md.
add_subdirectory(google_benchmarks)
