# ============================================================
# Build configuration for the regression.
# ============================================================

# Custom framework — see custom/README.md.
add_subdirectory(custom)

# Google framework — see google_regressions/README.md.
add_subdirectory(google_regressions)