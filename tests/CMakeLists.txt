# ============================================================
# Build configuration for the test suites.
# ============================================================

# Custom framework suite — see custom/README.md.
add_subdirectory(custom)

# GoogleTest suite — see google_tests/README.md.
add_subdirectory(google_tests)
